global {
  int x = 5;
}

main() bool {
  local int y = 2; # Expected next token to be {, got IDENT instead

  if y > x { # Expected next token to be (, got IDENT instead
    write("y número é maior que x!!");
  } else {
    write("y número é menor que x!!");
  }

  main = true;
}
